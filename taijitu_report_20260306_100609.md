# TAIJITU RED — Engagement Report
**Target:** http://testphp.vulnweb.com
**Date:** 2026-03-06 10:02 UTC
**Duration:** 208 seconds
**Total Findings:** 16
**Estimated Bounty:** $2,100 - $39,000

## Attack Surface
- IPs: 2
- Subdomains: 0
- Pages Crawled: 15
- Parameters Found: 12
- Forms: 3

## AI Guidance
### Guardian
ASSESSMENT: What you see in this situation
The testphp.vulnweb.com website has a high number of vulnerabilities, including critical SQL Injection and Reflected XSS findings. The site is missing several security headers, including Strict-Transport-Security, Content-Security-Policy, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, and Permissions-Policy. Additionally, sensitive files are exposed, and the server software (nginx) is outdated.

DEFENDER_WEAKNESS: What the defender missed or 

### Adversary
CHALLENGE: The Guardian overlooked the potential for chained attacks and failed to consider the broader implications of the missing security headers.

ATTACK_CHAIN:

1. **Missing Strict-Transport-Security Header**: This header can be used to redirect the browser to a secure connection (HTTPS). However, its absence allows an attacker to intercept sensitive data in transit.
2. **No Https**: Without HTTPS, the site is vulnerable to eavesdropping and man-in-the-middle attacks. An attacker could easi

### Consensus Next Step
NEXT ACTION: Implement Strict-Transport-Security (HSTS) header on the testphp.vulnweb.com website by adding the following header in the server configuration file (e.g., nginx.conf): `add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";`

---

# TAIJITU RED — Vulnerability Report
Generated: 2026-03-06 10:06 UTC
Total findings: 16

---

## CRITICAL (3 findings)

### SQL Injection in listproducts.php via cat parameter
**CVSS:** 9.8 | **Bounty:** $500 - $10,000+

A SQL injection vulnerability was identified in the 'cat' parameter at http://testphp.vulnweb.com/listproducts.php?cat=1. The application fails to properly sanitize user input before passing it to the database query, allowing an attacker to manipulate the query logic. Evidence: SQL error signature found: 'sql syntax'

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/listproducts.php?cat=1
2. Identify the 'cat' parameter
3. Replace the parameter value with payload: 
4. Observe the SQL error in the response
5. Confirm by trying: ' OR '1'='1 to bypass logic
6. Use sqlmap to extract database: sqlmap -u "{url}" -p {param} --dbs

**Impact:** An attacker can extract the entire database including user credentials, personal data, and sensitive business information. With write permissions, they can modify or delete data. In some configurations, SQL injection leads to remote code execution.

**Fix:** Use parameterized queries / prepared statements

---

### SQL Injection in artists.php via artist parameter
**CVSS:** 9.8 | **Bounty:** $500 - $10,000+

A SQL injection vulnerability was identified in the 'artist' parameter at http://testphp.vulnweb.com/artists.php?artist=1. The application fails to properly sanitize user input before passing it to the database query, allowing an attacker to manipulate the query logic. Evidence: SQL error signature found: 'sql syntax'

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/artists.php?artist=1
2. Identify the 'artist' parameter
3. Replace the parameter value with payload: 
4. Observe the SQL error in the response
5. Confirm by trying: ' OR '1'='1 to bypass logic
6. Use sqlmap to extract database: sqlmap -u "{url}" -p {param} --dbs

**Impact:** An attacker can extract the entire database including user credentials, personal data, and sensitive business information. With write permissions, they can modify or delete data. In some configurations, SQL injection leads to remote code execution.

**Fix:** Use parameterized queries / prepared statements

---

### SQL Injection in search.php via test parameter
**CVSS:** 9.8 | **Bounty:** $500 - $10,000+

A SQL injection vulnerability was identified in the 'test' parameter at http://testphp.vulnweb.com/search.php?test=query. The application fails to properly sanitize user input before passing it to the database query, allowing an attacker to manipulate the query logic. Evidence: SQL error signature found: 'sql syntax'

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/search.php?test=query
2. Identify the 'test' parameter
3. Replace the parameter value with payload: 
4. Observe the SQL error in the response
5. Confirm by trying: ' OR '1'='1 to bypass logic
6. Use sqlmap to extract database: sqlmap -u "{url}" -p {param} --dbs

**Impact:** An attacker can extract the entire database including user credentials, personal data, and sensitive business information. With write permissions, they can modify or delete data. In some configurations, SQL injection leads to remote code execution.

**Fix:** Use parameterized queries / prepared statements

---

## HIGH (4 findings)

### No Https in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

No Https vulnerability found at http://testphp.vulnweb.com in unknown parameter. SSL/TLS configuration issue detected

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This high severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Configure proper SSL/TLS settings

---

### Reflected XSS in listproducts.php via cat parameter
**CVSS:** 6.1 | **Bounty:** $200 - $3,000

A reflected cross-site scripting vulnerability was identified in the 'cat' parameter at http://testphp.vulnweb.com/listproducts.php?cat=1. User-supplied input is reflected in the response without proper encoding, allowing an attacker to inject malicious scripts. Evidence: Payload reflected unencoded in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/listproducts.php?cat=1
2. Identify the 'cat' parameter
3. Inject XSS payload: 
4. Observe the payload executing in the browser
5. Confirm by checking page source for unencoded payload

**Impact:** An attacker can steal session cookies to hijack authenticated user accounts, redirect users to phishing pages, perform actions on behalf of the victim, or deliver malware. Impact is highest when targeting admin users.

**Fix:** Encode all user input before reflecting in HTML

---

### Reflected XSS in artists.php via artist parameter
**CVSS:** 6.1 | **Bounty:** $200 - $3,000

A reflected cross-site scripting vulnerability was identified in the 'artist' parameter at http://testphp.vulnweb.com/artists.php?artist=1. User-supplied input is reflected in the response without proper encoding, allowing an attacker to inject malicious scripts. Evidence: Payload reflected unencoded in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/artists.php?artist=1
2. Identify the 'artist' parameter
3. Inject XSS payload: 
4. Observe the payload executing in the browser
5. Confirm by checking page source for unencoded payload

**Impact:** An attacker can steal session cookies to hijack authenticated user accounts, redirect users to phishing pages, perform actions on behalf of the victim, or deliver malware. Impact is highest when targeting admin users.

**Fix:** Encode all user input before reflecting in HTML

---

### Reflected XSS in showimage.php via file parameter
**CVSS:** 6.1 | **Bounty:** $200 - $3,000

A reflected cross-site scripting vulnerability was identified in the 'file' parameter at http://testphp.vulnweb.com/showimage.php?file=image. User-supplied input is reflected in the response without proper encoding, allowing an attacker to inject malicious scripts. Evidence: Payload reflected unencoded in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/showimage.php?file=image
2. Identify the 'file' parameter
3. Inject XSS payload: 
4. Observe the payload executing in the browser
5. Confirm by checking page source for unencoded payload

**Impact:** An attacker can steal session cookies to hijack authenticated user accounts, redirect users to phishing pages, perform actions on behalf of the victim, or deliver malware. Impact is highest when targeting admin users.

**Fix:** Encode all user input before reflecting in HTML

---

## MEDIUM (4 findings)

### Missing Strict-Transport-Security Header in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Missing Strict-Transport-Security Header vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Strict-Transport-Security' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Strict-Transport-Security' header to all responses

---

### Missing Content-Security-Policy Header in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Missing Content-Security-Policy Header vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Content-Security-Policy' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Content-Security-Policy' header to all responses

---

### Missing X-Frame-Options Header in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Missing X-Frame-Options Header vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'X-Frame-Options' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'X-Frame-Options' header to all responses

---

### Sensitive File Exposed: /admin/ in  via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Sensitive File Exposed: /admin/ vulnerability found at http://testphp.vulnweb.com/admin/ in unknown parameter. HTTP 200 response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/admin/
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Restrict access to sensitive files

---

## LOW (5 findings)

### Missing X-Content-Type-Options Header in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Missing X-Content-Type-Options Header vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'X-Content-Type-Options' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This low severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'X-Content-Type-Options' header to all responses

---

### Missing Referrer-Policy Header in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Missing Referrer-Policy Header vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Referrer-Policy' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This low severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Referrer-Policy' header to all responses

---

### Missing Permissions-Policy Header in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Missing Permissions-Policy Header vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Permissions-Policy' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This low severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Permissions-Policy' header to all responses

---

### Information Disclosure via Server in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Information Disclosure via Server vulnerability found at http://testphp.vulnweb.com in unknown parameter. Server: nginx/1.19.0

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This low severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Remove or obscure the Server header

---

### Information Disclosure via X-Powered-By in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** 

Information Disclosure via X-Powered-By vulnerability found at http://testphp.vulnweb.com in unknown parameter. X-Powered-By: PHP/5.6.40-38+ubuntu20.04.1+deb.sury.org+1

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This low severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Remove or obscure the X-Powered-By header

---
