# TAIJITU RED — Vulnerability Report
Generated: 2026-03-05 17:40 UTC
Total findings: 15

---

## CRITICAL (3 findings)

### SQL Injection in listproducts.php via cat parameter
**CVSS:** 9.8 | **Bounty:** $500 - $10,000+

A SQL injection vulnerability was identified in the 'cat' parameter at http://testphp.vulnweb.com/listproducts.php?cat=1. The application fails to properly sanitize user input before passing it to the database query, allowing an attacker to manipulate the query logic. Evidence: SQL error signature found: 'sql syntax'

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/listproducts.php?cat=1
2. Identify the 'cat' parameter
3. Replace the parameter value with payload: or true--
4. Observe the SQL error in the response
5. Confirm by trying: ' OR '1'='1 to bypass logic
6. Use sqlmap to extract database: sqlmap -u "{url}" -p {param} --dbs

**Impact:** An attacker can extract the entire database including user credentials, personal data, and sensitive business information. With write permissions, they can modify or delete data. In some configurations, SQL injection leads to remote code execution.

**Fix:** Use parameterized queries / prepared statements

---

### SQL Injection in artists.php via artist parameter
**CVSS:** 9.8 | **Bounty:** $500 - $10,000+

A SQL injection vulnerability was identified in the 'artist' parameter at http://testphp.vulnweb.com/artists.php?artist=1. The application fails to properly sanitize user input before passing it to the database query, allowing an attacker to manipulate the query logic. Evidence: SQL error signature found: 'sql syntax'

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/artists.php?artist=1
2. Identify the 'artist' parameter
3. Replace the parameter value with payload: or true--
4. Observe the SQL error in the response
5. Confirm by trying: ' OR '1'='1 to bypass logic
6. Use sqlmap to extract database: sqlmap -u "{url}" -p {param} --dbs

**Impact:** An attacker can extract the entire database including user credentials, personal data, and sensitive business information. With write permissions, they can modify or delete data. In some configurations, SQL injection leads to remote code execution.

**Fix:** Use parameterized queries / prepared statements

---

### SQL Injection in search.php via test parameter
**CVSS:** 9.8 | **Bounty:** $500 - $10,000+

A SQL injection vulnerability was identified in the 'test' parameter at http://testphp.vulnweb.com/search.php?test=query. The application fails to properly sanitize user input before passing it to the database query, allowing an attacker to manipulate the query logic. Evidence: SQL error signature found: 'sql syntax'

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/search.php?test=query
2. Identify the 'test' parameter
3. Replace the parameter value with payload: ' or true--
4. Observe the SQL error in the response
5. Confirm by trying: ' OR '1'='1 to bypass logic
6. Use sqlmap to extract database: sqlmap -u "{url}" -p {param} --dbs

**Impact:** An attacker can extract the entire database including user credentials, personal data, and sensitive business information. With write permissions, they can modify or delete data. In some configurations, SQL injection leads to remote code execution.

**Fix:** Use parameterized queries / prepared statements

---

## HIGH (3 findings)

### Reflected XSS in listproducts.php via cat parameter
**CVSS:** 6.1 | **Bounty:** $200 - $3,000

A reflected cross-site scripting vulnerability was identified in the 'cat' parameter at http://testphp.vulnweb.com/listproducts.php?cat=1. User-supplied input is reflected in the response without proper encoding, allowing an attacker to inject malicious scripts. Evidence: Payload reflected unencoded in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/listproducts.php?cat=1
2. Identify the 'cat' parameter
3. Inject XSS payload: <svg onload=alert(1)>
4. Observe the payload executing in the browser
5. Confirm by checking page source for unencoded payload

**Impact:** An attacker can steal session cookies to hijack authenticated user accounts, redirect users to phishing pages, perform actions on behalf of the victim, or deliver malware. Impact is highest when targeting admin users.

**Fix:** Encode all user input before reflecting in HTML

---

### Reflected XSS in artists.php via artist parameter
**CVSS:** 6.1 | **Bounty:** $200 - $3,000

A reflected cross-site scripting vulnerability was identified in the 'artist' parameter at http://testphp.vulnweb.com/artists.php?artist=1. User-supplied input is reflected in the response without proper encoding, allowing an attacker to inject malicious scripts. Evidence: Payload reflected unencoded in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/artists.php?artist=1
2. Identify the 'artist' parameter
3. Inject XSS payload: <svg onload=alert(1)>
4. Observe the payload executing in the browser
5. Confirm by checking page source for unencoded payload

**Impact:** An attacker can steal session cookies to hijack authenticated user accounts, redirect users to phishing pages, perform actions on behalf of the victim, or deliver malware. Impact is highest when targeting admin users.

**Fix:** Encode all user input before reflecting in HTML

---

### Reflected XSS in showimage.php via file parameter
**CVSS:** 6.1 | **Bounty:** $200 - $3,000

A reflected cross-site scripting vulnerability was identified in the 'file' parameter at http://testphp.vulnweb.com/showimage.php?file=image. User-supplied input is reflected in the response without proper encoding, allowing an attacker to inject malicious scripts. Evidence: Payload reflected unencoded in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/showimage.php?file=image
2. Identify the 'file' parameter
3. Inject XSS payload: alert&lpar;1&rpar;
4. Observe the payload executing in the browser
5. Confirm by checking page source for unencoded payload

**Impact:** An attacker can steal session cookies to hijack authenticated user accounts, redirect users to phishing pages, perform actions on behalf of the victim, or deliver malware. Impact is highest when targeting admin users.

**Fix:** Encode all user input before reflecting in HTML

---

## MEDIUM (9 findings)

### medium in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

medium vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Strict-Transport-Security' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Strict-Transport-Security' header to all responses

---

### medium in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

medium vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Content-Security-Policy' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Content-Security-Policy' header to all responses

---

### medium in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

medium vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'X-Frame-Options' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'X-Frame-Options' header to all responses

---

### medium in  via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

medium vulnerability found at http://testphp.vulnweb.com/admin/ in unknown parameter. HTTP 200 response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com/admin/
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Restrict access to sensitive files

---

### low in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

low vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'X-Content-Type-Options' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'X-Content-Type-Options' header to all responses

---

### low in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

low vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Referrer-Policy' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Referrer-Policy' header to all responses

---

### low in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

low vulnerability found at http://testphp.vulnweb.com in unknown parameter. Header 'Permissions-Policy' not present in response

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Add 'Permissions-Policy' header to all responses

---

### low in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

low vulnerability found at http://testphp.vulnweb.com in unknown parameter. Server: nginx/1.19.0

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Remove or obscure the Server header

---

### high in testphp.vulnweb.com via unknown parameter
**CVSS:** 5.0 | **Bounty:** Unknown

high vulnerability found at http://testphp.vulnweb.com in unknown parameter. SSL/TLS configuration issue detected

**Steps to reproduce:**
1. Navigate to http://testphp.vulnweb.com
2. Test the 'unknown' parameter with payload: 
3. Observe the vulnerability in the response

**Impact:** This medium severity vulnerability could allow an attacker to compromise the confidentiality, integrity, or availability of the application or its data.

**Fix:** Configure proper SSL/TLS settings

---
